# URL pública del backend, incluyendo el prefijo /api
NEXT_PUBLIC_API_URL=https://api.example.com/api

# Debe coincidir con JWT_SECRET_KEY del backend
JWT_SECRET_KEY=replace-with-a-long-random-secret
